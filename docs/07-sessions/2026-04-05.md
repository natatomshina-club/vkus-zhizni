# 🚀 Сессия 5 апреля 2026 — итоги
## Для вставки в начало новой сессии

---

## 📦 ТЕКУЩИЙ СТЕК

- **Frontend + Backend:** Next.js 14 + TypeScript → Docker контейнер на Beget VDS
- **БД:** Supabase self-hosted на том же VDS
- **Сервер:** 155.212.130.228 (4 CPU, 8 ГБ RAM)
- **Supabase Studio:** https://studio.nata-tomshina.ru
- **Клуб:** https://club.nata-tomshina.ru
- **Деплой:** `bash deploy.sh` из папки ~/Desktop/vkus-zhizni (НЕ vercel!)
- **SSH:** без пароля (ключ настроен)

### Команды деплоя
```bash
cd ~/Desktop/vkus-zhizni && bash deploy.sh
```

### Полезные команды на сервере
```bash
# Логи Next.js
docker logs vkus-zhizni --tail 50

# Перезапустить REST после изменений схемы БД
cd /opt/supabase/docker && docker compose restart rest
```

---

## ✅ ЧТО СДЕЛАНО В ЭТОЙ СЕССИИ

### Загрузка файлов
- Nginx — лимит `client_max_body_size 10M` в `/etc/nginx/sites-available/vkus-zhizni`
- Next.js — `experimental.serverActions.bodySizeLimit: '50mb'`
- 6 API роутов — добавлен `export const config` с bodyParser 50mb
- Сжатие фото в чате — canvas, макс 1200px, качество 0.8
- Лимит 10MB на фронте во всех компонентах загрузки

### Автоудаление фото из чатов (таблица channel_posts)
- `media_expires_at = now() + 72 hours` при отправке фото
- pg_cron задача каждый час — `cleanup-expired-media`
- CRON_SECRET: `vkus-zhizni-cron-2026`

### Обучающий тур
- Исправлена логика — `OnboardingTour.tsx` сам загружает данные через `/api/member/me`
- Тур показывается при `tour_completed = false` независимо от способа регистрации

### Docker — дисковое пространство
- Очищен build cache — освобождено 27GB (с 43GB до 16GB)
- В `deploy.sh` добавлена автоочистка: `docker builder prune -f && docker image prune -f`

### Марафоны — исправлено и добавлено
- Добавлены все недостающие колонки в таблицу `marathons`
- `slug` — необязательный, генерируется автоматически из title
- `announce_features` — тип изменён с `ARRAY` на `jsonb`
- Фичи «Что тебя ждёт» отображаются на странице участницы
- Кнопка «Напомни мне о старте» — исправлена, создана таблица `marathon_reminders`
- Убрано поле HTML рациона, список продуктов заменён на PDF (`shopping_list_pdf_url`)
- Bucket `marathon-files` — добавлены Storage policies
- Кнопка «Скачать список продуктов» работает в статусах `planned` и `active`

### Универсальный mailer.ts
- Создан `src/lib/mailer.ts` — единый модуль отправки email
- 8 файлов переведены на `sendEmail()` — OTP, рассылки, уведомления, партнёры
- **Для замены Resend → SMTP Beget менять только `mailer.ts`**

### Приветствие — имя вместо фамилии
- Добавлена колонка `first_name` в таблицу `members`
- Онбординг и профиль — три поля: Фамилия + Имя + Отчество
- Приветствие: `first_name` → fallback второе слово из `full_name`

### Email базы подписчиков
- Загружено 980 холодных подписчиков (Геткурс рассылка)
- Загружено 46 клубных из Геткурса (для приглашения в новый клуб)
- Импорт через `/admin/emails` работает

### Storage Policies
- `marathon-files` — SELECT, INSERT, DELETE
- `body-materials` — SELECT, INSERT, DELETE

### Раздел «Я и моё тело»
- Подпись на главной: «Памятки, Чек-листы» (было «Метод Натальи»)
- Карточки — название крупно на цветном фоне, иконки убраны
- Цвета по типу (пастельные полупрозрачные):
  - PDF → rgba(250, 220, 100, 0.35) — жёлтый
  - Видео → rgba(180, 170, 220, 0.35) — лавандовый
  - Статья → rgba(150, 210, 170, 0.35) — зелёный
  - Аудио → rgba(255, 182, 193, 0.35) — розовый

### Мобильные баги
- Избранные рецепты — 1 колонка на мобильном
- Тост-уведомления — не вылезают за край экрана
- Блок «Победы» — 5 плашек: Не перекусывала / Выпила 2 литра воды / Не хотелось сладкого / Отказалась от мучного / Хорошо спала

---

## ⚠️ НЕРЕШЁННЫЕ ПРОБЛЕМЫ

### 1. Календарь дневника — НЕ РАБОТАЕТ ❌
API `/api/diary/calendar` возвращает `{"markedDays":[],"feelingDays":[]}`.
Гипотеза: проблема в RLS или Kong при запросах от service_role.

### 2. Таймауты на /api/member/me ❌
ERR_TIMED_OUT периодически — связано с нагрузкой или Kong.

### 3. Мобильные баги (не исправлены)
- PDF iframe не влезает на мобильном
- HTML контент с таблицами вылезает за экран
- Бейдж цены вебинара обрезается
- Таблица статусов вебинаров обрезается

### 4. Куратор не видит марафоны

### 5. Чат: фото без текста — ошибка NOT NULL

### 6. CloudPayments — не протестирован
Ждём ответа от поддержки.

### 7. Домен nata-tomshina.ru не перенесён
Доступ к Reg.ru заблокирован, перенос в понедельник.

### 8. Материалы «Я и мое тело»
Ссылки ведут на старый supabase.co — нужно перезалить вручную через админку.

---

## 🔑 ВАЖНЫЕ ДАННЫЕ

```
Сервер: 155.212.130.228
SSH: root@155.212.130.228 (без пароля)
Supabase Studio: https://studio.nata-tomshina.ru
Клуб: https://club.nata-tomshina.ru
Локальный проект: ~/Desktop/vkus-zhizni
CRON_SECRET: vkus-zhizni-cron-2026
```

### Ключи Supabase self-hosted
```
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzAwMDAwMDAwLCJleHAiOjIwMDAwMDAwMDB9.dAbeVkLx3qXz6RRhhjZKATTWgX3LIMYrvUyrXtKSHBk
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoic2VydmljZV9yb2xlIiwiaXNzIjoic3VwYWJhc2UiLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAwMDAwMDAwMH0.1M7qu9D5dXl-dl2WTHpuvQvJeOXSriOfNLXK6tSpchU
```

---

## 📋 ПРИОРИТЕТЫ СЛЕДУЮЩЕЙ СЕССИИ

1. **Перенос домена** nata-tomshina.ru → Reg.ru (понедельник)
2. **Замена Resend → SMTP Beget** (после переноса домена, менять только `src/lib/mailer.ts`)
3. **CloudPayments** — тест платежей + защита от повторных триалов
4. **Исправить календарь дневника** — зелёные кружки
5. **Мобильные баги** — PDF/таблицы/вебинары
6. **Перезалить материалы** «Я и мое тело» через админку
7. **Подготовка к 300 участницам** — индексы БД, нагрузочное тестирование

---

## 📌 КАК РАБОТАЕМ

- Я (Claude в чате) формулирую ТЗ
- Ты отправляешь ТЗ в Claude Code (отдельный терминал)
- Claude Code редактирует файлы локально
- Деплой: `bash deploy.sh` из ~/Desktop/vkus-zhizni
- Файлы копируются на сервер через rsync, Docker пересобирается

**КРИТИЧЕСКОЕ ПРАВИЛО:** member_id везде берётся через lookup по email:
```typescript
const { data: member } = await supabaseAdmin
  .from('members').select('id').eq('email', user.email!).single()
// Использовать member.id, НЕ user.id!
```

**ВАЖНО — select('*') не подхватывает новые колонки** до перезапуска PostgREST.
Всегда писать явный список полей в запросах после добавления новых колонок!
