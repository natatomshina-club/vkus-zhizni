# 🚀 Сессия переезда на Beget — 4 апреля 2026 (v3)
## Статус: основные функции работают ✅

---

## ✅ ЧТО СДЕЛАНО СЕГОДНЯ

### База данных
- Пересоздана чистая схема с PRIMARY KEY и FOREIGN KEY на ВСЕ таблицы
- Почищены дубли во ВСЕХ таблицах
- Добавлены foreign keys: channel_posts → members, diary_entries → members, weekly_plans → members
- Добавлены foreign keys: recipes → recipe_ingredients, nutrition → recipe_ingredients
- Добавлены foreign keys: body_materials → body_sections
- Восстановлен удалённый курс «Стоп-диабет»
- Добавлен PRIMARY KEY на channel_last_seen (member_id, channel) — исправлена ошибка ON CONFLICT

### Supabase Studio
- Настроен постоянный доступ: https://studio.nata-tomshina.ru
- Логин: admin / пароль (тот что вводили при настройке htpasswd)
- SSH туннель больше не нужен ✅

### Авторизация
- Правильный способ создания участниц — через Admin API (curl), не через SQL
- shouldCreateUser изменён на true в auth/page.tsx

### Шаблон письма OTP ✅
- Красивый HTML шаблон работает
- Тема: «Ваш код для входа в клуб Вкус Жизни»
- Файл: /opt/supabase/docker/volumes/auth/templates/magic_link.html

### Что работает ✅
- Вход по OTP
- Умная кухня (рецепты подбираются)
- Чаты + фото в чатах
- Профиль + загрузка фото
- Вебинары + сообщение Наташе о покупке
- Курсы «Я и моё тело» (вводные курсы + материалы)
- Медитации
- FAQ
- Партнёры (аффилиаты)
- Админка — все разделы

### Storage Buckets созданы
- avatars, channel-media, body-materials, webinar-materials, meditation-audio, attachments

### Участницы в клубе
- nata.tomshina@gmail.com — admin, active, 10 лет
- jm-consult@mail.ru — admin, active, 1 год
- olga.tomshina@gmail.com — member, trial, 7 дней
- spectr58@mail.ru — member, trial, 7 дней
- anngela3@mail.ru — member, trial, 7 дней

---

## ⚠️ ПРОБЛЕМЫ — решить в следующей сессии

### 1. Файлы материалов не перенесены
Все PDF, видео, аудио остались в облачном Supabase.
Решение: добавить вручную через админку → Я и моё тело → Материалы.

### 2. CloudPayments — не протестирован
Тестовые платежи не проходят — обратились в поддержку CloudPayments.
Ключи настроены корректно в .env.

### 3. Закоммитить изменения в git
```bash
scp root@155.212.130.228:/root/app/src/app/\(club\)/auth/page.tsx ~/Desktop/vkus-zhizni/src/app/\(club\)/auth/page.tsx
scp root@155.212.130.228:/root/app/src/app/api/auth/check-member/route.ts ~/Desktop/vkus-zhizni/src/app/api/auth/check-member/route.ts
scp root@155.212.130.228:/root/app/next.config.ts ~/Desktop/vkus-zhizni/next.config.ts
scp root@155.212.130.228:/root/app/Dockerfile ~/Desktop/vkus-zhizni/Dockerfile
cd ~/Desktop/vkus-zhizni
git add -A && git commit -m "feat: Beget migration - auth OTP, check-member, shouldCreateUser"
git push
```

### 4. sync_member_id SQL функция — не создана
```sql
CREATE OR REPLACE FUNCTION sync_member_id(old_id uuid, new_id uuid, member_email text)
RETURNS void AS $$
BEGIN
  UPDATE payment_logs SET member_id = new_id WHERE member_id = old_id;
  UPDATE private_messages SET member_id = new_id WHERE member_id = old_id;
  UPDATE channel_posts SET member_id = new_id WHERE member_id = old_id;
  UPDATE diary_entries SET member_id = new_id WHERE member_id = old_id;
  UPDATE measurements SET member_id = new_id WHERE member_id = old_id;
  UPDATE wins SET member_id = new_id WHERE member_id = old_id;
  UPDATE members SET id = new_id WHERE id = old_id;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

### 5. Мелкие баги
- 404 на /dashboard/about (тур)
- Куратор не видит марафоны
- Чат: фото без текста — ошибка NOT NULL
- WebSocket Realtime иногда не подключается

### 6. Перенести nata-tomshina.ru на Beget
Нужен доступ к Reg.ru для смены NS серверов.

---

## 🔑 ВАЖНЫЕ ДАННЫЕ СЕРВЕРА

```
IP: 155.212.130.228
SSH: root@155.212.130.228
Supabase Studio: https://studio.nata-tomshina.ru (admin / пароль htpasswd)
```

### Ключи Supabase self-hosted
```
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzAwMDAwMDAwLCJleHAiOjIwMDAwMDAwMDB9.dAbeVkLx3qXz6RRhhjZKATTWgX3LIMYrvUyrXtKSHBk
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoic2VydmljZV9yb2xlIiwiaXNzIjoic3VwYWJhc2UiLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAwMDAwMDAwMH0.1M7qu9D5dXl-dl2WTHpuvQvJeOXSriOfNLXK6tSpchU
```

### Полезные команды
```bash
# Перезапустить Next.js
docker restart vkus-zhizni

# Логи Next.js
docker logs vkus-zhizni --tail 50

# Логи Supabase Auth
docker logs supabase-auth --tail 30

# Пересобрать и перезапустить Next.js
cd /root/app && docker build --no-cache \
  --build-arg NEXT_PUBLIC_SUPABASE_URL=https://club.nata-tomshina.ru/supabase \
  --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzAwMDAwMDAwLCJleHAiOjIwMDAwMDAwMDB9.dAbeVkLx3qXz6RRhhjZKATTWgX3LIMYrvUyrXtKSHBk \
  --build-arg NEXT_PUBLIC_SITE_URL=https://club.nata-tomshina.ru \
  --build-arg NEXT_PUBLIC_CLOUDPAYMENTS_PUBLIC_ID=pk_ef164a2e6a2ab9c21f55a72eb6d35 \
  -t vkus-zhizni . && \
docker stop vkus-zhizni && docker rm vkus-zhizni && \
docker run -d --name vkus-zhizni --restart unless-stopped \
  --network supabase_default \
  -p 3001:3000 --env-file /root/app/.env.production.local vkus-zhizni

# Перезапустить REST (после изменений схемы БД)
cd /opt/supabase/docker && docker compose restart rest

# Перезапустить Auth
cd /opt/supabase/docker && docker compose up -d --force-recreate auth

# Создать участницу (ПРАВИЛЬНЫЙ СПОСОБ — через Admin API!)
curl -X POST http://localhost:8000/auth/v1/admin/users \
  -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoic2VydmljZV9yb2xlIiwiaXNzIjoic3VwYWJhc2UiLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAwMDAwMDAwMH0.1M7qu9D5dXl-dl2WTHpuvQvJeOXSriOfNLXK6tSpchU" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoic2VydmljZV9yb2xlIiwiaXNzIjoic3VwYWJhc2UiLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAwMDAwMDAwMH0.1M7qu9D5dXl-dl2WTHpuvQvJeOXSriOfNLXK6tSpchU" \
  -H "Content-Type: application/json" \
  -d '{"email":"EMAIL_ЗДЕСЬ","email_confirm":true}'
# Скопировать UUID из ответа и выполнить в SQL:
# INSERT INTO members (id, email, name, role, subscription_status, tariff, subscription_ends_at, subscription_expires_at)
# VALUES ('UUID', 'email', 'Имя', 'member', 'trial', 'trial', now() + interval '7 days', now() + interval '7 days');
```

---

## 📝 ВСТУПИТЕЛЬНОЕ СООБЩЕНИЕ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ

Привет! Продолжаем переезд клуба «Вкус Жизни» на Beget.

**Контекст:** Next.js 14 + Supabase self-hosted на Beget VDS.

**Текущий статус:**
- Сервер: 155.212.130.228 (4 CPU, 8 ГБ RAM) ✅
- Supabase Studio: https://studio.nata-tomshina.ru ✅
- Вход по OTP + красивое письмо ✅
- Умная кухня ✅
- Чаты + фото ✅
- Вебинары, курсы, медитации ✅
- Все PRIMARY KEY добавлены, дубли почищены ✅

**Проблемы для решения:**
1. Загрузить файлы материалов вручную через админку
2. CloudPayments — ждём ответа поддержки
3. Закоммитить изменения в git
4. Создать sync_member_id SQL функцию
5. Мелкие баги: 404 about, куратор, чат фото без текста

Прикладываю SESSION_MIGRATION файл и PROJECT_BRAIN_v9.md.
