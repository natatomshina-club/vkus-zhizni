# SESSION 2026-05-20 (final) — Handoff для следующей сессии

> Прикрепить к первому сообщению новой сессии.
> Продолжение `SESSION_2026-05-20_late_VAULT_HANDOFF.md`.

---

## 📊 Что сделано за сессию

### 1. Vault — два новых модуля

**`02-public-site/analytics.md`** — аналитика и трекинг:
- Яндекс.Метрика (ID 108262096) — единственный внешний счётчик
- Внутренний трекинг `page_views` через `Analytics.tsx` (только `view` работает, остальные события не реализованы — R110)
- `klub_diagnostics` — лид-форма диагностики, не аналитика
- Consent/cookie banner отсутствует (R113)
- Таблица защиты клуба: все 6 пунктов ✅ (включая новый `YandexMetrika.tsx`)

**Правки по итогам разведки:**
- Создан `src/components/public/YandexMetrika.tsx` — Client Component, возвращает `null` на `/dashboard/*` и `/admin/*`
- `Analytics.tsx` — ранний выход на `/dashboard/*` и `/admin/*`
- `layout.tsx` — inline-скрипт Метрики (35 строк) заменён на `<YandexMetrika />`

### 2. R102 — скрыты незаполненные разделы

Убраны из навигации, футера, sitemap и robots.txt:
- `/recipes` — убран из футера и sitemap, добавлен `Disallow` в robots.txt
- `/menyu` — убран из nav и футера, добавлен `Disallow` в robots.txt
- `/partner` — убран из футера, `Disallow` уже был. Роут сохранён — вернуться когда будет готов
- `/racion` — нигде не светился, добавлен `Disallow` для порядка

**R103 закрыт автоматически** — sitemap рецептов убран вместе с разделом.

### 3. R50 — subscription_plan при ручном апгрейде

Исправлены три места в двух файлах:
- `members/[id]/route.ts` PATCH блок `tariff` — добавлен `subscription_plan`
- `members/[id]/route.ts` PATCH блок `extend_days` — добавлен `subscription_plan`
- `members/route.ts` POST upsert — добавлен `subscription_plan`

Теперь `getEffectiveMonths()` считает бонусные 6 месяцев для полугодовых участниц корректно.

### 4. R76 закрыт — баг был в документации

В БД колонка `sweet_craving` (не `craving`). Код верен, `TRACKER_IMPL.md` содержал неверное имя. `measurements.md` исправлен.

### 5. R96 переатрибутирован

`proxy.ts` к Rule #11 не относится — занимается только аффилиатными ref-кодами.
Реальная картина: 14 файлов ✅ правильный паттерн `.eq('email', user.email)`, 42 файла ❌ неправильный `.eq('id', user.id)`.
Не ломает сейчас (единственный админ), приоритет снижен до среднего.

### 6. Правила деплоя — зафиксированы в CLAUDE.md и CLAUDE-RULES.md

- Деплой = `bash deploy.sh` на Mac (rsync → docker build → restart)
- Не `git pull` на сервере — у сервера локальные изменения поверх старого коммита
- После правок: коммит → push → deploy.sh → проверить `docker ps`

### 7. Деплой прода

Все изменения задеплоены. Прод на коммите `e2a425f` и далее.
Контейнер `vkus-zhizni` Up, порт 3001→3000.

---

## 🗂 Состояние Vault — полная картина

### Готово ✅

**Клубные модули** (`03-club/modules/`):
meditations, webinars, smart-kitchen, meal-plans, marathons, subscriptions,
diary, measurements, wins, affiliate, channel, courses, recipes, profile

**Дизайн клуба:** `03-club/design-system.md`

**Админские модули** (`04-admin/modules/`):
emails, members, help-admin, marathons-admin, webinars-admin,
stories-admin, decoration-admin

**Инфраструктура** (`05-infrastructure/`):
server, payments, storage, email-system, database

**Публичный сайт** (`02-public-site/`):
site-routing, design-system, seo-structure, blog-infrastructure,
content-publishing, **analytics** ← новое

**Операции** (`06-operations/`):
manual-procedures

**Roadmap:** R01–R113

### Технический Vault — завершён ✅

Все технические модули клуба, админки, инфраструктуры и публичного сайта задокументированы.

---

## 🎯 Цель следующей сессии — Второй Vault

Наташа хочет создать **продуктово-маркетинговую базу знаний** — отдельно от технического Vault.

Это НЕ технический Vault (Claude Code его не читает). Это стратегический мозг проекта для Наташи.

**10 разделов:**
1. Лендинги — стиль, язык, скилы
2. Воронка продаж + методика продаж в переписках
3. Реклама (включая марафоны)
4. Истории/кейсы — как создавать
5. Соцсети и Дзен
6. CRM для обработки заявок (будущее)
7. Email-рассылки — логика сбора, куда попадают, как включать серии
8. SEO — всё
9. Дорожная карта клиента + уроки клуба
10. На будущее: партнёрка, доп. продукты, SERM

**Где хранить:** отдельный Obsidian Vault вне Git (`~/Desktop/Клуб-маркетинг/` или аналог).

**Стартовое сообщение для новой сессии:**
```
Хочу создать второй Vault — продуктово-маркетинговый мозг проекта.
10 разделов (список в handoff). Помоги спроектировать структуру
и создать скелет.
```

---

## ⚠️ Открытые баги — текущая картина

### Высокий приоритет
- **R96** (переименован) — 42 API-роута используют `.eq('id', user.id)` вместо `.eq('email', user.email)` в `requireAdmin`. Механическая правка — отдельная сессия.

### Средний приоритет
- R51 — метка «Заблокированные» для `cancelled` семантически неверна
- R52 — нет глобального rate limiter в `send-announcement`
- R57 — GoTrue SMTP-провайдер требует уточнения
- R58 — два email-шаблона расходятся
- R71 — `member_id` vs `user.id` в diary роутах
- R78 — `kbju_manual` сбрасывается при замере веса
- R83 — расхождение стартового веса между трекером и победами
- R110 — `trackEvent()` в Analytics.tsx определена, нигде не вызывается
- R111 — миграция для `page_views` отсутствует
- R113 — нет cookie consent / 152-ФЗ баннера

### Низкий приоритет
R53, R59, R60, R63, R65–R75, R77, R79–R82, R84–R89, R100, R104–R106, R108, R109, R112

---

## 📌 Методика — без изменений

1. Код = первый источник истины
2. ТЗ → разведка в чате → ОК → запись → `git status` → коммит → push → deploy.sh
3. `git status` после каждого шага — только ожидаемые файлы
4. Никаких изменений на проде без явного «делай»
5. Один модуль за итерацию
6. Секреты не пишем — только имена переменных
7. Попутные находки → `_findings.md`
8. Коммитим логическими блоками, push + деплой в конце
9. Handoff пишет основной Claude (в чате), не Claude Code
10. **Деплой = `bash deploy.sh` на Mac. Никогда не `git pull` на сервере.**
